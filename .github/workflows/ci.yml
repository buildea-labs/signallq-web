name: CI

on:
  push:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test
      - run: npm run build

      # O e2e não rodava em lugar nenhum: era gate só de nome, verificado à
      # mão. Agora roda aqui, contra o build de produção que o próprio
      # `playwright.config.ts` levanta — o artefato que vai ao ar.
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
