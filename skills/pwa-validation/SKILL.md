---
name: pwa-validation
description: Validate the SignallQ Web manifest, service worker, install prompt, and update behavior.
---

# PWA validation

Build first. Confirm manifest and icons are served, service worker registers, install behavior is guarded, and an update does not reload without user action.
